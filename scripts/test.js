const daysUntil = "There are " + daycount + " days until " + targetEvent + "!";
const days1Until = `There are  ${daycount}  days until ${targetEvent} !`;
today2.innerHTML = `<strong>Volume</strong>: ${volume} liters`; 